<!DOCTYPE html>
<html>


  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= product.name  %> </title>
    <!-- Font Awesome Stylesheet -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.11.2/css/all.min.css">
      <!-- Bootstrap Grid Stylesheet -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/css/bootstrap.min.css" rel="stylesheet" >
    <!-- Own Stylesheet -->
    <link rel="stylesheet" href="/stylesheets/style.css">
    <link rel="stylesheet" href="/stylesheets/app.css">
    </head> 
  
    <!-- Header -->
 <%- include('./partials/header')  %> 
	<!-- /Header -->
	
	<!-- Banner -->
	<div class="banner"></div>
	<!-- /Banner -->

  <!--utilizo la función para convertir a miles-->
  <% const toThousand = n => n.toString().replace(/\B(?=(\d{3})+(?!\d))/g, ".");  %> 
  


  <!-- Ofertas -->
  <!--Del controlador viene el objeto product y utilizo sus propiedades-->
	<div class="container products-wrapper">
		<div class="row">
			<div class="col-12">
				<h2 class="products-title">Detalle</h2>
			</div>
        <div class="col-12 col-sm-6 col-lg-3">
          <section class="product-box">
              <figure class="product-box_image">
                <img src="/images/products/<%= product.image  %> " alt="imagen de producto">
              </figure>
              <article class="product-box_data">
                <h2><%= product.name%></h2>
                <h3>$<%= toThousand(product.priceDiscount) %></h3>
                <% if (product.discount > 0) { %>
                  <span class="line-through">$<%=toThousand(product.price)%></span>
                  <span><%= product.discount%>% OFF</span>
                <% } %> 
                <p><%= product.description%></p>
                <i class="fas fa-truck"></i>
              </article>
            </a>
          </section>
        </div>	
		</div>
	</div>
	<!-- /Ofertas -->






	

	<!-- footer -->
  <%- include('./partials/footer')  %> 
  </body>
</html>
